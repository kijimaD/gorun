jobs:
  job_a:
    description: test
    steps:
      - name: a
        run: echo helloA
      - name: run version
        run: go version
      - name: run version
        run: go build .
      - name: help
        run: go fmt ./...
  job_b:
    description: test
    steps:
      - name: a
        run: echo helloB
